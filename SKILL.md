---
name: job-apply
description: "Use globally in any Codex conversation when the user asks about 秋招、校招、正式校招岗位、管培生、网申、投递简历、校招官网、投递规则、投递台账、内推 or campus recruitment, including requests to search company career sites, screen suitable roles, provide official job links, fill applications, or track submissions for themselves or a student (学员)."
---

# job-apply — 秋招投递

## 全局可用性

- 本技能安装在 `~/.codex/skills/job-apply`，供任意 Codex 对话全局发现和调用；不要把流程限定在创建或更新技能时所在的对话。
- 用户可显式说“调用 job apply”或使用 `$job-apply`；也可通过秋招、校招、网申、查岗位、投递简历等自然语言隐式触发。
- 每个新对话都从本技能的 `config.json`、`references/` 和 `records/` 重建上下文，不依赖其他对话的聊天记录。长期偏好、官网链接库和投递台账必须写入本技能目录后才视为跨对话生效。
- 跨对话只复用已明确保存的偏好和事实；短信验证码、页面临时状态及未经用户确认的信息不得跨对话保存。

## 首次使用与隐私边界

- 这是可公开分享的通用版本，不包含技能作者或分享者的姓名、手机号、邮箱、学校、简历路径、登录资料、投递记录或投递跟踪表链接。
- 首次进入网申阶段时，若 `records/` 中还没有当前用户的个人信息文件，提示用户复制 `references/personal-info-template.json` 创建自己的资料文件；查岗位阶段不因此阻塞。
- 若 `records/applications.jsonl` 不存在，在首次记账前创建空文件；该文件属于当前用户的本地隐私数据，不应提交到公开仓库。
- 首次需要登录且 `config.json` 中手机号为空时，当场向当前用户询问手机号。只有用户明确要求长期保存时才写入配置；短信验证码永不保存。
- 首次需要同步投递台账时，先读取 `config.json.tracking`。只有 `enabled=true` 且 `table_url` 是当前用户明确提供或确认属于自己的表格时，才允许访问。
- 若当前用户没有自己的飞书多维表格，必须提示其新建后提供链接，或继续只使用本地 `records/applications.jsonl`。不得打开、引用或写入技能作者、分享者、教程截图、示例文档中的任何飞书表格。
- 推荐用户自己的表格至少包含：`公司名称`、`事业部&岗位`、`岗位类型`、`投递时间`、`进度`、`岗位链接`。字段或选项未建立时先指导用户配置，不擅自往未知表格写入。

## 概述

统一编排「ego lite 查岗位 → 按偏好筛选 → 询问是否扩展范围 → 提供链接 → 用户批准岗位和简历 → 填表复核 → 确认提交 → 记账」的秋招网申流程,并处理「登录招聘官网查询投递进度 → 向用户汇报 → 同步飞书多维表格」的进度跟踪。铁律四条:查岗和查进度首先使用 ego lite;链接只从 `references/companies.md` 查(不凭记忆造);未获用户对具体岗位和具体简历的批准不得开始网申;投递前必查台账、投递后必记账(`records/applications.jsonl`)。

## 分阶段收集信息

- **查岗阶段**:只需意向公司;岗位方向和城市未临时指定时读取 `config.json` 的长期偏好,不要因为尚未选择简历或缺少个人信息而阻塞查岗。
- **网申阶段(缺一不可)**:用户明确批准的公司与具体岗位、用户明确选择的简历文件(.docx/.pdf/.md)、学员姓名、个人信息文件(JSON,模板见 `references/personal-info-template.json`)、城市选择及提交模式。缺省读取 `config.json` 的 `submit_mode`;用户临时指定则仅本次生效。

## 工作流

1. **打开 ego lite** — 用户要求查询某公司岗位时,第一步必须使用 ego-browser 操作 ego lite。复用同一查岗任务空间并打开该公司官方校招站;不要改用内置浏览器、Chrome 或普通 Web 搜索代替主要查岗流程。
2. **官网缺失/失效确认门** — 若 `references/companies.md` 未收录该公司、链接失效或公司主体存在歧义,先明确告诉用户具体问题,再在 ego lite 内搜索「<公司全称> 校招官网」。用户位于中国大陆:候选入口按“中国大陆官方招聘站/中文官网或中国区页面 > 全球官网的中国岗位页 > 海外官网”排序,优先提供在中国大陆可访问、面向中国大陆招聘且使用中文的官方链接;不得因公司使用全球品牌而默认发送海外入口。核验公司主体、官方域名和招聘批次,并从候选公司的官网“关于我们”、产品或业务页面提取主营业务、主要产品/服务和目标市场,用 1–3 句话描述公司业务;无法从官网确认的内容明确标注“官网未说明”,不得凭印象补充。把候选官网链接、公司业务简介及主体判断依据一并发给用户确认;若只能找到全球或海外入口,明确说明未找到中国大陆入口及使用该链接的原因。用户明确确认官网前,不得筛选岗位、回写 `companies.md`、进入申请表或执行后续动作。遇到同名公司、主体不确定、登录墙、验证码或风控时立即暂停并询问用户,不得猜测或绕过。用户确认后才回写已验证链接并继续。
3. **筛选岗位** — 从 `references/companies.md` 取得已收录或经用户确认后回写的官方校招链接并进入岗位列表。链接与岗位默认优先中国大陆:先查中文校招页面、中国大陆招聘项目及工作地点在中国大陆的岗位;全球或海外岗位仅在用户明确要求、长期偏好包含海外方向,或大陆入口信息不足时补充,并清楚标注地区。先读取 `config.json` 的 `job_preferences`,再结合用户本次要求与城市筛选。默认只保留正式校招岗位或管培生/培训生岗位中的非技术产品经理/用户产品经理、产品运营/商业化运营、海外非技术岗以及电商相关非技术岗;排除所有实习、日常实习、暑期实习岗位,并排除研发、算法、工程、测试、技术运维和设计岗。必须结合职位名称、招聘项目/批次和详情页判断岗位性质;只要标注“实习”“实习生”或属于实习招聘项目,就不得进入候选结果。岗位名称含 AI、数据或平台时以实际职责判断。输出岗位标题、方向、城市、要求摘要、届别和岗位性质(正式校招/管培生),不要在此阶段开始申请。只有用户本次明确要求实习岗位时,才临时解除实习排除规则。
4. **核实并主动汇报投递规则** — 在公司当前官方校招 FAQ、招聘公告或投递系统中核实本批次的投递限制。无论用户是否主动询问,每次查询公司岗位后都必须在首轮岗位结果中单列“投递规则”,逐项报告:①常规最多可投岗位数;②是否存在额外投递机会;③额外机会是否占用常规名额;④提交后能否修改简历;⑤志愿能否修改、撤回或重投;⑥工作城市能否修改。每项都要注明“官方已核实”或“官方未说明/尚未核实”,不得凭经验推断。不同项目或批次规则不一致时分别说明,不得混用。
5. **展示结果、链接并确认范围** — 完成初筛后,在同一条结果消息中先用 1–3 句话描述该公司的主营业务、主要产品/服务和目标市场,再展示匹配岗位、已核实的投递规则,并为每个候选岗位主动附上已验证可打开的官方详情页或申请页直达链接;公司业务描述必须以本次在 ego lite 中核实的官网内容为依据。不得等用户再次索要链接,也不得用只有首页、搜索页或无法定位具体岗位的链接冒充岗位链接。随后明确询问“是否还需要查看其他岗位/方向”;若用户要求扩展,继续在同一 ego lite 任务空间筛选,并对新增结果同样附上直达链接。
5. **等待双重批准** — 发出链接后等待用户明确选择“哪一个具体岗位”和“哪一份具体简历”。在两项均获批准前,不得打开申请表、上传附件、填写信息或触发自动解析。若用户只批准其中一项,只询问缺少的另一项。
6. **届别检查与查重** — 获得岗位和简历批准后,用个人信息或简历中的毕业时间核对招聘批次:正式批只收当年应届(如 2027 届 = 2026.09–2027.08 毕业)。不匹配时暂停并提示该批次不符,改查匹配届别的正式校招或非技术管培生;不得主动推荐实习岗。在 `records/applications.jsonl` 检索学员+公司(+岗位);已投过则暂停询问是否仍要继续。
7. **登录招聘系统** — 读取 `config.json` 的 `login_preferences`。网站支持手机号登录时,默认优先使用已保存手机号;需要短信验证码时,必须先填写手机号、勾选必要协议并实际点击“获取验证码/发送验证码”,再通过按钮倒计时、发送成功提示或页面其他明确状态确认验证码已触发发送,确认后才把 ego lite 交给用户并暂停,由用户在页面填写验证码和完成登录。不得在尚未点击发送或无法确认已触发时就交接;若发送前出现图形验证码、滑块、风控或其他必须由用户完成的验证,则立即交接并说明需要完成的步骤。验证码只允许保存在当前对话的短期上下文中,不得写入 SKILL、配置、个人信息 JSON、台账、日志或任何文件。若网站不支持手机号登录,再使用其可用方式并在需要扫码、验证码或风控操作时把 ego lite 交给用户。
8. **填表并回写资料** — 打开已批准岗位的投递页并上传已批准简历。若页面提供「简历自动解析」「解析覆盖」等功能,默认先点击并等待解析完成,让系统生成基础表单;再按表单字段从个人信息 JSON 和简历复核、补全、纠错。字段含义不确定(如「户籍」vs「现居」)暂停询问,不猜。关键字段的可信来源优先级为:用户本次明确确认 > 个人信息 JSON > 本次指定简历 > 自动解析结果 > 网页已有值/浏览器自动填充。自动解析或网页旧值与高优先级来源冲突时,必须覆盖并向用户报告冲突。教育、实习、项目等经历也要按简历逐项核对,不得因自动解析成功而跳过。用户明确指定“当前所在地按岗位 Base 填写”时,以该岗位官方详情页标注的工作地点映射当前所在地;官网只给到省市时只填可确认的层级,不得臆造区县,并在填写后确认环节明确展示该映射。用户在网申过程中明确补充或纠正个人资料时,同步回写该学员的个人信息 JSON;只保存用户明确确认的事实,不把猜测、临时网页值或账号验证码写入文件。姓名、学校、学历、专业、毕业时间、手机、邮箱、意向城市每次填完后必须逐项回读页面实际值;任何一项无法回读或不一致都不得保存或提交。简历附件用 ego-browser 的 `uploadFile`。附件格式/大小不符合要求的,先用 docx/pdf 工具转换。
8. **填写后确认** — 每完成一批字段填写或纠错,向用户展示该批字段的页面实际值及与个人信息 JSON/简历的差异,等待用户明确确认后再进入下一批或“预览并提交”。即使 `submit_mode` 为 `auto`,也不得跳过字段填写结果的确认;`auto` 只免除最终提交按钮前的再次确认。
9. **提交** — 按提交模式:
   - `confirm`(默认):填完展示核对清单(岗位 / 姓名 / 学校 / 学历 / 专业 / 毕业时间 / 手机 / 邮箱 / 简历文件 / 城市),用户确认后才点提交按钮
   - `auto`:填完直接提交
   - `manual`:填完由用户自己在浏览器点提交
10. **记账** — 追加一行到 `records/applications.jsonl`:`{"time","student","company","position","url","status","mode"}`。status 取值:`submitted`(已提交)/ `ready`(已填完等用户确认)/ `manual`(用户自行提交)/ `failed`(失败+原因)。
11. **更新用户自己的投递跟踪表** — 默认在确认岗位已提交后更新;若用户明确要求提前记录尚未投递的具体岗位,也可提前写入。先检查 `config.json.tracking.enabled` 与 `table_url`:未启用或链接为空时,明确提示“尚未配置你自己的飞书多维表格”,请用户创建并提供链接,或选择只记入本地台账;不得寻找或使用他人的表格。已配置时用 ego lite 打开该链接,按“公司名称 + 岗位类型 + 投递时间 + 进度”定位记录。先只读查重并向用户展示拟写入或拟修改的值,获得用户明确批准后才写入。写入时只能使用表内已有字段;单选/多选字段只能选择已有选项,不得新建字段或选项。没有完全一致的岗位类型时,选择已有选项中语义最接近的一项;若无法可靠判断最接近项,先列出候选项请用户确认。进度字段默认映射:岗位尚未提交为“待投递”;已提交但尚无后续通知为“简历筛选”;收到测评通知为“待测评”;收到 AI 面试通知为“待AI面”。如果当前用户表内没有这些选项,先提示其建立或确认映射,不得擅自新增。提前记录时,“投递时间”填写用户明确计划的日期;用户未给出日期或日期有歧义时先确认。若四项完全一致的记录已经存在,不新增、不覆盖;若存在同公司同岗位但时间或进度不同的记录,说明差异并等待用户批准后更新对应行。
12. **汇报** — 本次投递结果 + 飞书跟踪表状态(已更新/已有一致记录/等待批准) + 该学员台账摘要(已投哪些公司、什么状态) + 本批次投递限制(常规名额、额外机会及是否占名额、简历/志愿能否修改)。

## 查询投递进度并同步飞书

当用户明确要求查询某公司或若干公司的投递进度时,先查询招聘官网并汇报。只有当前用户已配置自己的投递跟踪表并授权同步时,才继续更新表格:

1. 用 ego lite 复用同一进度查询任务空间,从 `references/companies.md` 取得该公司官方招聘入口并进入个人中心/申请记录;不得用普通 Web 搜索或其他浏览器替代。
2. 复用已有登录状态。遇到登录、短信验证码、扫码、验证码或风控时,把任务空间交给用户并暂停;用户明确回复“继续”后才重新接管。
3. 对每条申请读取页面实际显示的公司、岗位、投递时间和当前进度;同时记录页面能确认的更新时间或提示。不得仅凭邮件、旧台账或经验推断官网状态。
4. 先向用户汇报官网结果,再读取 `config.json.tracking`。若未配置当前用户自己的表格,停止表格同步并提示用户创建飞书多维表格后提供链接;不得使用分享者或示例表格。若已配置且用户已授权,用 ego lite 打开 `table_url`,按“公司名称 + 岗位类型 + 投递时间”定位唯一记录并更新“进度”。
5. 只能更新匹配记录的既有字段并选择表内已经存在的选项。严禁创建任何新字段、新选项或重复记录,也不得把招聘官网的原始状态文本直接新增为飞书选项;不清楚该选哪个已有选项时必须先询问用户。完全一致时不写入;匹配到多条、无法唯一匹配、官网状态含义不明或没有可靠对应选项时,列出差异并暂停询问用户。
6. 进度映射沿用本技能现有规则:已投递且无后续通知为“简历筛选”,收到测评通知为“待测评”,收到 AI 面试通知为“待AI面”。阿里巴巴/阿里健康招聘官网显示“评估中”时固定映射为飞书“简历筛选”。官网显示其他阶段时,只在表内已有语义明确对应项时更新;否则先请用户确认。
7. 更新后回读多维表格的公司名称、岗位类型、投递时间和进度,确认写入成功;最终汇报每条申请的官网状态、表格更新前后值,以及未更新项和原因。

## 浏览器操作要点(ego-browser 0.4.6 实测)

本机 ego-browser 为 0.4.6.14,与 ego-browser skill 文档(1.2.x)有行为差异,实测可用模式:

- 开空间导航:`useOrCreateTaskSpace(name)` → `listTabs()` 取 `targetId` → `switchTab(targetId)` → `gotoUrl(url)`
- **`openOrReuseTab` 和 `wait()` 在本版本会挂起,禁用**;等待用 `await new Promise(r => setTimeout(r, ms))`
- 读页面状态:`listTabs()`(url/title)、`snapshotText()`(页面文本)、`js()`(页面内 JS)
- 输出走 `cliLog()`;heredoc 输出只在进程退出时刷新,脚本末尾加 `process.exit(0)`
- 脚本里用 `require` 时需包裹 async 函数,或改用 `const fs = await import('fs')`(顶层 await 与 require 不能混用)
- **登录墙 / 验证码 / 风控**(字节、腾讯等校招页对自动化较严)→ 暂停,请用户在 ego lite 窗口手动完成登录后继续

## 修改提交模式

- `config.json`:`{"submit_mode": "confirm"}`
- 用户说「这次直接投」→ 仅本次用 auto;说「以后都自动投」→ 改 config.json
- 改完在回复中确认当前生效的模式

## 修改岗位偏好

- 长期偏好保存在 `config.json` 的 `job_preferences`
- 用户说「以后只看/也看/不看某类岗位」→ 更新 `job_preferences`
- 用户说「这次找某类岗位」→ 仅本次覆盖,不修改全局偏好
- 岗位名称含 AI、数据或平台不等于技术岗;以职位类别和实际职责判断是否属于产品、运营或业务岗

## 常见错误

- 凭记忆/猜测校招官网 URL → 只用 companies.md;未收录、失效或主体有歧义时先告知用户,再用 ego lite 搜索并展示候选官网和判断依据,用户明确确认后才回写和继续
- 找到官网或岗位后只发链接、不介绍公司业务 → 同步从官网核实并简述主营业务、主要产品/服务和目标市场;官网未说明的内容不猜
- 面向中国大陆用户却优先发送全球或海外链接 → 先找中国大陆官方招聘站、中文页面和中国大陆岗位;只能使用海外入口时说明原因
- 不查台账直接投 → 重复投递可能进企业黑名单
- 查岗位时未先打开 ego lite,或用其他浏览器代替 → 查岗任务第一步固定使用 ego lite
- 只列岗位名称、等用户追问才发链接 → 每次展示候选岗位时同步主动附上已验证的官方直达链接,然后询问是否还需查看其他岗位/方向
- 把实习生岗位混入正式校招结果 → 默认只保留正式校招或管培生/培训生;职位名称、招聘项目或详情页任一处标注实习时立即排除,除非用户本次明确要求实习
- 用户未追问就省略投递规则,或只列岗位不核实限制 → 每次查岗结果都主动单列规则清单,同步汇报常规名额、额外机会及是否占名额、简历能否修改、志愿能否修改/撤回/重投、城市能否修改;官方未说明的逐项明确标注
- 用户只选了岗位、未选简历就开始网申(或反之)→ 必须同时获得具体岗位与具体简历的批准
- 网站支持手机号却改用其他登录方式、未先实际触发验证码就交接,或保存短信验证码 → 优先使用配置中的手机号;先点击“获取验证码/发送验证码”并确认倒计时或成功提示后再交接;验证码必须当次使用且绝不落盘
- 不核对毕业时间与批次 → 2028 届投 2027 届正式批 = 无效投递(基线实测踩过)
- 表单字段靠猜(把「现居地」填进「户籍地」)→ 不确定就问
- 用户要求当前所在地跟随岗位 Base 时擅自补全区县 → 只映射岗位官网明确给出的省市层级,缺少区县时不猜并展示给用户确认
- 信任网页旧资料或浏览器自动填充 → 它们只能作参考,必须用用户确认/个人信息 JSON/本次简历覆盖并回读校验
- 只检查输入动作、不检查页面最终值 → 所有关键字段必须从页面回读一致后才能保存或提交
- 用户补充资料后只填网页、不回写个人信息 JSON → 将明确确认的新资料同步保存,验证码和未确认值不得落盘
- 填完后直接进入预览或下一批字段 → 先展示页面实际值及差异并等待用户确认
- 上传简历后跳过自动解析、或直接信任解析结果 → 默认先自动解析,再依据用户确认/个人信息 JSON/简历逐项复核纠错
- confirm 模式下没等用户确认就点提交 → 投递不可逆,必须等
- 未查重或未经批准就写飞书投递跟踪表 → 先用 ego lite 按四项字段查重并展示拟更新值;尚未投递时仅在用户明确要求提前记录的情况下使用已有“待投递”选项;已有一致记录时不写,存在差异时获批后才更新
- 在飞书投递跟踪表中创建新字段或新选项 → 只能复用表内已有字段和已有选项;无完全匹配时选语义最接近的已有项,无法可靠判断时先请用户确认
- 将已投递岗位的飞书进度写成“已投递” → 尚无后续通知时选“简历筛选”;收到测评通知后改为“待测评”;收到 AI 面试通知后改为“待AI面”

## 文件结构

- `references/companies.md` — 企业校招官网链接库(223 家,16 类;文件顶部有分类索引,可锚点跳转)
- `references/personal-info-template.json` — 个人信息 JSON 模板(每学员一份)
- `references/form-filling-guide.md` — 校招表单填写规范与踩坑点
- `records/applications.jsonl` — 投递台账(追加式,勿删)
- `config.json` — 提交模式配置
